'use client'

/**
 * @deprecated This component has been replaced by EditorPreview which renders
 * actual HTML content in an iframe. Kept for backwards compatibility.
 */
export const PreviewFrame = () => {
  return null
}
