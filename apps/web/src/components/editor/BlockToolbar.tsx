'use client'

/**
 * @deprecated Block toolbar functionality has been integrated into
 * the Select to Edit mode in EditorPreview. Kept for backwards compatibility.
 */
export const BlockToolbar = () => {
  return null
}
